# Wave 1: Agent B may use this if the learner calls a real model.
# A deterministic fixture fallback is required when the key is missing.

OPENAI_API_KEY=
# or
GEMINI_API_KEY=
# or
ANTHROPIC_API_KEY=
