# `fixtures/` — Agent B

Runtime copies and learner I/O. Start from [shared/examples/](../shared/examples/) (read-only). Copy those JSON files here if the learner needs local paths.

Screenshot paths in `shared/examples/trace.store-a.json` may be placeholders. Do not block Wave 1 on real PNGs.
