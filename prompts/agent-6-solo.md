You are the solo Wave 6 agent. Do 6A fully, then 6B.

1. Follow prompts/agent-6a.md until `python -m agent.run_transfer` discovers campaign promo after shipping and second run already knows LAUNCH10.

2. Then follow prompts/agent-6b.md (Modal parallel A/B for the promo mismatch, with local fallback).

Do not start Store C or photo input. Next.js on http://localhost:3010. Local agent.

For Modal: user may need `modal setup` once; your code must still pass without it.
