# `adapters/` — Agent B (Wave 1 samples only)

Persist one file per app + skill. Wave 1 writes a **sample** recovered adapter:

```text
adapters/store_b__publish_product.json
```

Copy from [shared/examples/adapter.recovered.json](../shared/examples/adapter.recovered.json). No live executor writes here until Wave 2.
